<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.teamsai.saibackend.domain.settlement.mapper.SettlementAbandonmentAlertMapper">

    <insert id="insertIfAbsent">
        INSERT IGNORE INTO settlement_abandonment_alert (settlement_id, reference_date)
        VALUES (#{settlementId}, #{referenceDate})
    </insert>

</mapper>