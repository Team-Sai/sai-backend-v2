<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.teamsai.saibackend.domain.link.mapper.LinkMapper">

    <resultMap id="userResultMap"
               type="org.teamsai.saibackend.domain.user.dto.UserDTO">

        <id property="userId" column="user_id"/>

        <result property="userToken" column="user_token"/>
        <result property="userKey" column="user_key"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="birthDate" column="birth_date"/>

    </resultMap>

    <update id="updateUserKey">
        UPDATE users
        SET user_key = #{userKey}
        WHERE user_id = #{userId}
        AND user_key IS NULL
    </update>
</mapper>