<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.teamsai.saibackend.domain.account.mapper.LinkedBankAccountMapper">

    <select id="selectLinkedAccountsByUserId" resultType="org.teamsai.saibackend.domain.account.dto.LinkedBankAccountDTO">
        SELECT
        linked_account_id,
        user_id AS userId,
        account_id AS accountId,
        account_number AS accountNumber,
        bank_code AS bankCode,
        account_holder_name AS accountHolderName,
        connection_status AS connectionStatus,
        balance AS balance,
        created_at AS createdAt
        FROM linked_bank_account
        WHERE user_id = #{userId}
        <!-- 현재 활성 상태인 것만 소유권으로 인정할지는 나중에 정함 -->
    </select>

    <select id="selectAvailableLinkedAccountsByUserId"
            resultType="org.teamsai.saibackend.domain.account.dto.LinkedBankAccountDTO">
        SELECT
        linked_account_id,
        user_id AS userId,
        account_id AS accountId,
        account_number AS accountNumber,
        bank_code AS bankCode,
        account_holder_name AS accountHolderName,
        connection_status AS connectionStatus,
        balance AS balance,
        created_at AS createdAt
        FROM linked_bank_account
        WHERE user_id = #{userId}
        AND connection_status = 'AVAILABLE'
    </select>

    <select id="findAllAvailableForSync" resultType="org.teamsai.saibackend.domain.account.dto.LinkedAccountSyncTargetDTO">
        SELECT linked_account_id AS linkedAccountId, user_id AS userId
        FROM linked_bank_account
        WHERE connection_status = 'AVAILABLE'
        ORDER BY linked_account_id
    </select>

    <insert id="insertOne" useGeneratedKeys="true" keyProperty="linkedAccountId">
        INSERT INTO linked_bank_account (
        user_id, account_id, bank_code, account_number,
        account_alias, account_holder_name, balance,
        connection_status, created_at, updated_at
        ) VALUES (
        #{userId}, #{accountId}, #{bankCode}, #{accountNumber},
        #{accountAlias}, #{accountHolderName}, #{balance},
        #{connectionStatus}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <select id="findById" resultType="org.teamsai.saibackend.domain.account.dto.LinkedBankAccountDTO">
        SELECT
        linked_account_id,
        user_id AS userId,
        account_id AS accountId,
        account_number AS accountNumber,
        bank_code AS bankCode,
        account_holder_name AS accountHolderName,
        connection_status AS connectionStatus,
        balance AS balance,
        created_at AS createdAt
        FROM linked_bank_account
        WHERE linked_account_id = #{linkedAccountId}
    </select>

    <select id="findLastSyncedTransactionIdById" resultType="java.lang.Long">
        SELECT last_synced_transaction_id
        FROM linked_bank_account
        WHERE linked_account_id = #{linkedAccountId}
    </select>

    <update id="updateLastSyncedTransactionId">
        UPDATE linked_bank_account
        SET last_synced_transaction_id = #{transactionId}
        WHERE linked_account_id = #{linkedAccountId}
        AND (last_synced_transaction_id IS NULL OR last_synced_transaction_id &lt; #{transactionId})
    </update>

    <update id="updateBalance">
        UPDATE linked_bank_account
        SET balance = #{balance},
            updated_at = NOW()
        WHERE linked_account_id = #{linkedAccountId}
    </update>
</mapper>
